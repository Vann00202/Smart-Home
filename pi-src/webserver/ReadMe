#This command copy ssh key from command prompt to raspberry pi ssh authorized_keys folder

type C:\Users\Vann7\.ssh\id_ed25519.pub | ssh zero2@10.60.0.243 "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"

Scp -r dist/project_name/* /var/www/html

Rename /var/www/html/index.html file to index.html.bak

From browser folder, move all items to /var/www/html
sudo cp ~/my-app/* /var/www/html/

Give apache access to files using
sudo chown -R www-data:www-data /var/www/my-site

Pip install flask
Pip3 install flask_cors

asdf-data:www
